# Port used by the gRPC server. Can be overridden with the PORT env var (upstream-compatible).
adservice.port=${PORT:9555}
# Service name reported as SERVING by grpc.health.v1.Health.
adservice.health.service-name=hipstershop.AdService

spring.main.banner-mode=off
